# ADR-`<NNNN>`: `<Short decision title, in the imperative>`

Copy to `SPEC/decisions/<NNNN>-<kebab-title>.md`, using the next free number.

<!-- Delete every instruction comment before committing. One page maximum. -->

| | |
|---|---|
| Status | `<Proposed / Accepted / Superseded by ADR-NNNN / Deprecated>` |
| Date | `<YYYY-MM-DD>` |
| Deciders | `<who>` |
| Supersedes | `<ADR-NNNN, or none>` |

> **Decision principle** (`AGENTS.md` standing instruction 3). Do not weight development cost
> heavily. Rank options by quality, simplicity, robustness, scalability and long-term
> maintainability. "Faster to build" is not a sufficient reason and should not appear alone in
> the Alternatives table; "simpler to reason about in two years" is decisive.

## Context

<!-- The forces at play: the constraint, the problem, what was already true. Written so someone
with no memory of the conversation understands why a decision was needed at all. No solution
here. -->

`<context>`

## Decision

<!-- One or two sentences, active voice: "We will ...". Specific enough to be falsifiable. -->

We will `<decision>`.

## Consequences

| | |
|---|---|
| Positive | `<what this makes easy or cheap>` |
| Negative | `<what this makes hard, and the cost accepted>` |
| Follow-up needed | `<work this creates, or none>` |

## Alternatives considered

| Alternative | Why not |
|---|---|
| `<option>` | `<the real reason rejected, not a strawman>` |
